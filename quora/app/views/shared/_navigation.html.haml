.nav
  %span=link_to "Post a question", new_question_path, { :class => "colorbox btn btn-large btn-success" }
  -if current_user
    %span= link_to "My Profile", user_path(current_user)
    %span= link_to "Sign Out", signout_path
  -else
    %span= link_to "Sign In", new_session_path
    %span= link_to "Sign Up", new_user_path